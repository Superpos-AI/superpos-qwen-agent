# superpos-agent-core is the shared runtime — pulled from GitHub so this repo
# can build standalone.  Pin to a specific commit/tag for reproducible
# builds; @main is fine during active development.
superpos-agent-core @ git+https://github.com/Superpos-AI/superpos-agent-core.git@a84c88d103953bfec018a503efbdf7a3042d838e

# Transitive deps of superpos-agent-core listed here for IDE/lockfile clarity.
python-telegram-bot>=21.0
httpx>=0.27.0
pyyaml>=6.0
