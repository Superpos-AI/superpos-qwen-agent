description: "Read and write the Superpos knowledge store — FTS / semantic search, list, get, graph traversal, topic & decision indexes, plus bounded create/update for recording lasting decisions inline."
env:
  - SUPERPOS_BASE_URL
  - SUPERPOS_HIVE_ID
  - SUPERPOS_AGENT_ID
  - SUPERPOS_API_TOKEN
  - SUPERPOS_REFRESH_TOKEN
deps:
  python: []
  system: []
